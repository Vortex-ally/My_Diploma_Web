[tool.black]
line-length = 88
target-version = ["py313"]

[tool.ruff]
line-length = 88
target-version = "py314"

[tool.ruff.lint]
select = [
    "E",
    "F",
    "I",
]

ignore = [
    "E501", # довгі рядки
    "F403", # from .base import *
]

exclude = [
    "*/migrations/*",
]

[tool.ruff.lint.per-file-ignores]
"volunteer/volunteer/settings/*.py" = ["F405"] # names come from `from .base import *`

[tool.ruff.format]

[tool.isort]
profile = "black"
skip_glob = [
    "*/migrations/*",
]

[tool.pytest.ini_options]
DJANGO_SETTINGS_MODULE = "volunteer.settings.testing"

python_files = [
    "tests.py",
    "test_*.py",
    "*_tests.py",
]